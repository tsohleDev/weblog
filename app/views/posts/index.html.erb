<%= render 'shared/user', user: @user%>

<%= render partial: 'shared/post', collection: @posts[0...3], locals: { truncate: true, comments: true }%>

<div class='flex justify-around'>
  <%= link_to 'Pagination', user_posts_path(user_id: @user.id), class: 'm-auto p-3 text-[1.5em] font-bold bg-slate-600 hover:bg-slate-900 hover:text-white' %>
<div>
