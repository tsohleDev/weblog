<%= render 'shared/user', user: @user%>

<article>
    <h2 class="text-[3em] font-bold">Bio</h2>
    <p class="text-[1.5em] border-l-[10px] px-2 border-blue-200 self-end inline-block"> <%= @user.bio %> </p>
</article>

<%= render partial: 'shared/post', collection: @posts, locals: { truncate: true, comments: false}%>

<div class='flex justify-around'>
  <%= link_to 'See All Posts', user_posts_path(user_id: @user.id), class: 'm-auto p-3 text-[1.5em] font-bold bg-slate-600 hover:bg-slate-900 hover:text-white' %>
<div>
